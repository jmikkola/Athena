fn main() {
    let n = 600851475143
    let largest = 0
    let factor = 2
    while n >= factor {
        if n % factor == 0 {
            largest = factor
            n = n / factor
        } else {
            factor = factor + 1
        }
    }
    print(largest)
}
