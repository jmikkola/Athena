type Bar struct {
    x Int
    y Float
}

type Foo struct {
    asdf Int
    xyz Bar
}

fn main() {
    let foo Foo = Foo{
        asdf: 123 + 345,
        xyz: Bar{
            x: 1,
            y: 2.333,
        },
    }
    let b Bar = foo.xyz
    print(String(foo))
    print("Hello, world")
    foo.xyz.x = 10000
    print("an Int: " + String(foo.xyz.x + 9))
    print("a Float: " + String(foo.xyz.y * 0.6))
}
